{
  "name": "text-to-audio-app",
  "version": "1.0.0",
  "description": "Text to Speech application with AI voices",
  "main": "api/server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"cd api && npm run dev\" \"cd client && npm run dev\"",
    "start": "cd api && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
